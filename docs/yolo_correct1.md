細菌解析フォームの修正をお願いします。
- AI解析で検出されたBoxに、「G+球菌」などの文字表示をしていますが、感じが文字化けして??になっています。日本語フォントで埋め込むよう確認を。
- カウント、検出領域のリストボックスで項目を選択したら、該当するボックスのみを表示するか、あるいはそれを囲うようにサークルを出すか、検討して。複数選択もできると嬉しい。また全表示、あるいはリセットボタンを設置し、これを押すと今まで通り全部の検出領域を表示するようにしたい。

以下は新機能の追加です。
教師データ作成モードに、自動tile分割機能を追加してください。

元画像から640×640のtileを自動作成し、strideは512px、つまり128px overlapを初期値にしてください。
tile候補を順番に表示し、ユーザーが以下を選べるようにしてください。

1. このtileをラベル編集する
2. このtileを空ラベルとして保存する
3. このtileをスキップする
4. 次のtileへ進む
5. 前のtileへ戻る

保存する場合は、crop画像を images/trainに保存し、
labels/trainにYOLO形式txtを保存し、
meta/trainにjsonを保存してください。

meta.jsonには元画像ファイル名とtile座標を保存してください。

tile設定:
  tile_size: 640
  stride: 512
  overlap: 128

手動crop機能は残してください。